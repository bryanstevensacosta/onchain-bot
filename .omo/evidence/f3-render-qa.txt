F3: RENDER TEST QA
==================

Date: 2026-06-23
Scope: F3 (Real QA) — render-level verification of human-readable labels
Method: @testing-library/react + jsdom render tests against the real DOM
Working dir: /Users/bryanstevens/dev/onchain-bot

SCENARIO 1: ScoreBreakdown renders labels
[✓] All render tests pass: 7/7 (apps/frontend/src/entities/token-score/ui/score-gauge.test.tsx)
    Evidence: .sisyphus/evidence/f3-score-gauge-render.log
    Coverage:
      - "No holders" rendered for SIGNAL_NO_HOLDERS
      - "Possible rug pull" rendered for SIGNAL_POSSIBLE_RUG
      - "High Liquidity" rendered for LIQUIDITY_HIGH (preserved entry)
      - DOM never contains raw codes for any of the 9 SIGNAL_* scoring factors
      - Fallback: "Future thing" rendered for unknown SIGNAL_FUTURE_THING
      - Fallback: "Made up code" rendered for MADE_UP_CODE (no prefix)
      - Multiple factors all render their human labels

SCENARIO 2: RejectedTable renders labels
[✓] All render tests pass: 6/6 (apps/frontend/src/features/reprocess-rejected/ui/rejected-table.test.tsx)
    Evidence: .sisyphus/evidence/f3-rejected-table-render.log
    Coverage:
      - "Score too low" and "Blacklisted" rendered (human-readable)
      - DOM never contains any of 7 raw reason codes (SCORE_TOO_LOW, BLACKLISTED, etc.)
      - All 7 human labels present (Score too low, Blacklisted, Honeypot suspected,
        Chain unsupported, Insufficient data, Risk weight exceeded, Classification blocked)
      - Table tree renders without error; row contains Reprocess button
      - Loading state renders "Cargando…"
      - Empty state renders "No hay tokens rechazados."
      - SKIP recommended renders "—" placeholder (no reprocess button)

SCENARIO 3: Fallback works
[✓] Tests pass: 2/2 (filter "humanized" in score-gauge)
    Evidence: .sisyphus/evidence/f3-fallback-render.log
    Coverage:
      - SIGNAL_FUTURE_THING → "Future thing" (signalLabel fallback)
      - MADE_UP_CODE → "Made up code" (reasonLabel fallback via humanize)

VERIFICATION
[✓] npm test: 46 tests passed (33 existing signalLabels + 13 new render tests)
    Was 33 → now 46 (33 unit + 7 ScoreBreakdown + 6 RejectedTable)
    Evidence: .sisyphus/evidence/f3-render-test.log
[✓] npm run build: exit code 0 (188 modules transformed, 343.17 kB bundle)
    Evidence: .sisyphus/evidence/f3-build.log
[✓] npm run lint: exit code 0 (no issues)
    Evidence: .sisyphus/evidence/f3-lint.log

DOM VERIFICATION
[✓] NO raw enum codes in DOM
    - SIGNAL_NO_HOLDERS, SIGNAL_POSSIBLE_RUG, SIGNAL_NO_NAME,
      SIGNAL_LOW_LIQUIDITY, SIGNAL_NO_PAIRS, SIGNAL_CONCENTRATED_HOLDERS,
      SIGNAL_EXTREME_PRICE_CHANGE, SIGNAL_MICROCAP, SIGNAL_NO_MARKET_DATA
      → all absent from DOM (asserted via container.textContent)
    - SCORE_TOO_LOW, BLACKLISTED, HONEYPOT_SUSPECTED, CHAIN_UNSUPPORTED,
      INSUFFICIENT_DATA, RISK_WEIGHT_EXCEEDED, CLASSIFICATION_BLOCKED
      → all absent from DOM
[✓] Human-readable labels present
    - "No holders", "Possible rug pull", "No token name", "Low liquidity",
      "No trading pairs", "Concentrated holders", "Extreme price change",
      "Micro-cap", "No market data" (9 SIGNAL_* factors)
    - "Score too low", "Blacklisted", "Honeypot suspected", "Chain unsupported",
      "Insufficient data", "Risk weight exceeded", "Classification blocked"
      (7 reason codes)
[✓] Fallback works
    - Unknown SIGNAL_ code → "Future thing" (humanized)
    - Unknown code without prefix → "Made up code" (humanized)
    - Preserved scoring factors still work (LIQUIDITY_HIGH → "High Liquidity")

TEST INFRASTRUCTURE NOTES
- Existing test infrastructure was preserved: vite.config.ts and src/test/setup.ts
  were NOT modified.
- Per-file pragma `// @vitest-environment jsdom` at top of both new test files
  enables the jsdom environment (otherwise vitest defaults to node).
- `import '@/test/setup';` in each file loads jest-dom matchers
  (.toBeInTheDocument).
- `afterEach(cleanup)` from @testing-library/react was required to reset the
  jsdom DOM tree between tests (v16 does not auto-clean).

FILES CREATED
- apps/frontend/src/entities/token-score/ui/score-gauge.test.tsx
- apps/frontend/src/features/reprocess-rejected/ui/rejected-table.test.tsx

FILES UNTOUCHED (verified via git diff)
- apps/frontend/src/shared/lib/signalLabels.ts
- apps/frontend/src/entities/token-score/ui/score-gauge.tsx
- apps/frontend/src/features/reprocess-rejected/ui/rejected-table.tsx
- vite.config.ts
- src/test/setup.ts
- package.json

VERDICT: APPROVE
================

The signal-labels hotfix is verified at the real-DOM level. Both ScoreBreakdown
and RejectedTable render human-readable labels in every test scenario:

  • Raw enum codes never appear in the rendered DOM (negative assertions hold)
  • Human-readable labels are present in the DOM (positive assertions hold)
  • Fallback path humanizes unknown codes correctly
  • Preserved entries (LIQUIDITY_HIGH, SIGNAL_HONEYPOT) still work
  • All existing 33 unit tests still pass
  • Production build succeeds
  • Lint passes

Total test count: 33 → 46 (+13 render tests). No regressions. No new dependencies.
No changes to the components or to signalLabels.ts.
