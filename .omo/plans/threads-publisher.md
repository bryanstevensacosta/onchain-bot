# threads-publisher - Work Plan

## TL;DR (For humans)
<!-- Fill this LAST, after the detailed plan below is written, so it summarizes the REAL plan. -->
<!-- Plain English for a non-engineer: NO file paths, NO todo numbers, NO wave/agent/tool names. -->

**What you'll get:** Un publicador de Threads completo (backend + página web de control) que reutiliza tu maquinaria de Crypto News: mismas palabras clave/listas, misma cola con límites, mismo asistente de redacción, pero publicando en Threads en vez de Telegram.

**Why this approach:** Copiar-y-renombrar en vez de abstraer, porque tus dos destinos van a divergir y tu repo ya prefiere duplicar antes que acoplar; y todo dentro del backend existente, porque Threads no necesita app separada (ya lo probamos en vivo con el spike).

**What it will NOT do:** No creará servicios ni bases de datos nuevos, no tocará nada de Crypto News, no hará fotos/videos todavía (solo texto), y no publicará en tu cuenta real sin tu confirmación.

**Effort:** Large
**Risk:** Medium - son ~150 ficheros nuevos pero clonados de código probado; el riesgo vive en cuotas de Meta y el token de 60 días
**Decisions to sanity-check:** Límite diario 60 posts (margen bajo los 250 de Meta), publicación cada 10 minutos, token guardado en tabla propia con refresco automático, y sección web separada en vez de fusionada.

Your next move: aprueba y lo ejecutamos con `$start-work`, o dime qué ajustar. Full execution detail follows below.

---

> TL;DR (machine): Large/Medium — Threads publisher as backend BCs (clone+rename, adapter+token new) + /threads frontend section, 8 todos, tests-after.

## Scope
### Must have
- `src/threads/publisher/` (backend BC espejo crypto-news-publisher): 8 entidades `threads_*` (queue, keywords, blacklist, llm_config SIN target, prompt_templates, throttle_state, matching_configs 1 row, oauth_tokens id=1); `EnqueueThreadsMessageUseCase` (`THREADS_MAX_QUEUE_DEPTH = 100`); `ProcessNextThreadsArticleUseCase` (dailyCap default 60, prompt exige <500, adapter trunca+…); cron drain `*/10 * * * *` lock `THREADS_PUBLISHER_ADVISORY_LOCK_ID=7_421_372`; janitor TTL 24h→FAILED; throttle bounds 60s–300s vía SharedThrottleSchedulerService; 5 controllers (`threads-publisher/keywords|blacklist|phrases|queue|llm`) + guard prod 400 en PATCH llm; status 6-estados paridad (PENDING|SCHEDULED|PUBLISHING|PUBLISHED|FAILED|BLOCKED).
- `ThreadsApiPublisherAdapter` (2-step `POST /{uid}/threads` → poll `?fields=status` 3s×10 → `POST /{uid}/threads_publish`; truncate 500 pre-publish; strip media + log `media_skipped`) + `ThreadsTokenRefresher` (cron diario, refresh si <7d para expirar).
- `src/threads/integration/`: `ThreadsMatchingConfig` (1 row, matchingEnabled) + `GET|PATCH threads/matching/config` + `GET threads/matching/health`; `FilteredThreadsService` (fetch ingestion HTTP → ContentFilterService reusado por puerto → keywords/threads AND-groups → blacklist → enqueue); handler SSE reusando `messageType='crypto-news'` (cero cambios ingestion-service); polling scheduler espejo.
- Config: `AppConfig.threads` + `THREADS_ACCESS_TOKEN/USER_ID/POLLING_INTERVAL_MINUTES/DAILY_CAP` placeholders vacíos en `.env.staging.template` + `.env.production.template` + validator; alias `threads/*` tsconfig + jest mapper; `PERSISTED_ENTITIES` + `EXPECTED_ENTITY_COUNT 39→47`; migración `1875000000001-CreateThreadsPublisherTables` (IF NOT EXISTS, down inverso).
- Frontend `/threads`: `entities/threads` (keys `threadsPublisherKeys`/`threadsMatchingKeys`, prohibido `['threads']` desnudo; messages/sources reusados de ingestion-api; filtros reusados); `features/threads-publisher` api/model + wrappers finos `Threads*` con inyección `basePath`+keys (NO as-is); página hub 6 bloques espejo; route+nav; `ENDPOINTS.threads`; proxy Vite `/threads-publisher` + `/threads/matching` (nunca `/threads` desnudo) + nginx mirrors; hide toggle LLM si `VITE_APP_ENV==='production'`.
- Tests-after espejo por todo (specs backend Jest + frontend Vitest `threads-page.test.tsx` con assert de URLs `/threads-publisher/*`); `docs/threads-token-runbook.md` (T3) + `docs/threads-go-no-go.md` (T8) con test -f + grep.
### Must NOT have (guardrails, anti-slop, scope boundaries)
- NO `apps/threads-publisher/`, NO DB propia, NO Dockerfile/compose/workflow/proxy de app, NO SSE propio, NO slot arbitrator compartido (drain independiente; concurrent Telegram+Threads mismo minuto permitido por diseño), NO tocar tablas `crypto-news-ads/*`.
- NO modificar `telegram/crypto-news-*/`, `telegram/ingestion/` (import-only; QA `git diff --stat` debe mostrar cero cambios ahí), NO `apps/ingestion-service`, NO tocar controllers/frontend crypto-news existentes.
- NO ads, NO media/carrusel (strip+log), NO App Review, NO multi-cuenta, NO `npm install` (fetch nativo), NO lock distinto de 7_421_372, NO `fields=status_code`, NO `['threads']` desnudo, NO bare `/threads` en proxy.
- NO rechazo en enqueue por longitud (cola guarda raw; guarda solo pre-publish); NO commitear tokens (`git diff --check` + grep QA); NO publicar en productiva sin confirmación; NO compartir entidades entre BCs (excepción única: filters reusado por puerto).

## Verification strategy
> Zero human intervention - all verification is agent-executed.
- Test decision: **tests-after** + Jest (backend, `DATABASE_ENABLED=true`) + Vitest (frontend). Cada todo escribe implementación + specs espejo en el MISMO todo.
- Evidence: `.omo/evidence/task-<N>-threads-publisher.<ext>` (stdout + exit codes; gitignored, no commitear).
- Red-ban: con tokens `FAKE`/ausentes o flags mock, `fetch('https://graph.threads.net*')` PROHIBIDO en QA agent (aplica a todos + F3; EXCEPCIÓN ÚNICA: F4-humano opt-in). Migración real (`migration:run`) PROHIBIDA en todos (solo `migration:show` + `--dry-run`); la única `migration:run` vive en F3 contra dev DB. Post real en productiva vetado siempre.
- Comandos canónicos: `cd apps/backend && npx tsc --noEmit` (exit 0), `DATABASE_ENABLED=true npx jest src/threads --forceExit`, `npm run migration:show`, `cd apps/frontend && npx tsc --noEmit` + `npx vitest run src/pages/threads src/features/threads-publisher`, `git diff --stat -- apps/backend/src/telegram apps/ingestion-service` (vacío) + `git diff --stat -- apps/backend/src/shared` → solo `common/persistence/entities.ts`, `common/config/app.config.ts`, `common/config/config-validator.ts`, `git diff --check`.

## Execution strategy
### Parallel execution waves
- Wave 1 (scaffold, 1 todo — prerrequisito físico de todo lo demás): T1 entidades + PERSISTED_ENTITIES(47) + migración + alias tsconfig/jest.
- Wave 2 (2 en paralelo): T2 application core + T3 adapter/token/config (ambos solo dependen T1, ficheros disjuntos).
- Wave 3 (3 en paralelo, disjuntos): T4 HTTP (1,2,3) + T5 integration (1,2 +contrato DAILY_CAP T2, sin token) + T6 LLM (1,2).
- Wave 4 (2 en paralelo contra contrato pineado inline en T7, arranca solo con Wave 3 en verde): T7 frontend data + T8 frontend ui/página/proxy (T8 no espera código de T7, solo el contrato + rutas backend vivas de T4/T5).
- Final: F1–F5 en paralelo. Metis ya corrió (17 gaps plegados); doble revisión opencode en curso (momus+oracle).
### Dependency matrix
| Todo | Depends on | Blocks | Can parallelize with |
| 1 scaffold+entities+migration+alias | — | 2,3,4,5,6 | — |
| 2 publisher application | 1 | 4,5,6 | 3 |
| 3 adapter+token+config | 1 | 4 | 2 |
| 4 publisher HTTP | 1,2,3 | 7,8 | 5,6 |
| 5 integration BC | 1,2 (+contrato DAILY_CAP de T2; enqueue/filter no usan token) | 7,8 | 4,6 |
| 6 LLM adapter+templates | 1,2 | — | 4,5 |
| 7 frontend data | 4,5 (contratos) | — (contrato→8) | 8 |
| 8 frontend ui+página+proxy | 4,5 (rutas vivas) + contrato T7 | F1–F5 | 7 |

## Todos
> Implementation + Test = ONE todo. Never separate.
<!-- APPEND TASK BATCHES BELOW THIS LINE WITH edit/apply_patch - never rewrite the headers above. -->
- [x] 1. Scaffold src/threads + alias + 8 entidades + PERSISTED_ENTITIES(47) + migración
  What to do / Must NOT do: Crear `apps/backend/src/threads/publisher/` y `apps/backend/src/threads/integration/` con DDD espejo (domain/entities, application/, infrastructure/persistence/typeorm/{entities,repositories,mappers}, api/http). 8 entidades threads_*: `threads_queue_entries` (status 6-estados PENDING|SCHEDULED|PUBLISHING|PUBLISHED|FAILED|BLOCKED + cols espejo queued_at/matched_keyword_ids/generated_*/blocked_reason/duplicate_of_* + partial idx WHERE status='PENDING'), `threads_keywords`, `threads_blacklist_phrases`, `threads_llm_configs` (SIN target; llm_enabled/publishing_enabled/reject_non_latin/dailyCap/delays/llmMaxAttempts), `threads_prompt_templates`, `threads_throttle_states`, `threads_matching_configs` (1 row, matchingEnabled), `threads_oauth_tokens` (id=1: access_token, threads_user_id, obtained_at, expires_in_s). Registrar en `shared/common/persistence/entities.ts` + bump `EXPECTED_ENTITY_COUNT 39→47`. Migración `1875000000001-CreateThreadsPublisherTables` (IF NOT EXISTS, down inverso oauth_tokens→matching→throttle→templates→llm→blacklist→keywords→queue, sin media). Alias `"threads/*": ["src/threads/*"]` en `apps/backend/tsconfig.json:paths` + entrada `moduleNameMapper` Jest. MUST NOT: tocar `telegram/*`, crear controllers/use-cases (todos 2-6), `synchronize:true` en prod, secretos en migración/seeds (placeholders vacíos).
  Parallelization: Wave 1 | Blocked by: — | Blocks: 2,3,4,5,6
  References (executor has NO interview context - be exhaustive): espejos `apps/backend/src/telegram/crypto-news-publisher/domain/entities/*.entity.ts` (keyword.entity.ts:21, blacklist-phrase.entity.ts:22, llm-config.entity.ts:24 cols :39-63, prompt-template.entity.ts:29, publisher-queue.entity.ts:26 cols :76,:80,:143,:157 + status :6-16, publisher-throttle-state.entity.ts:14), `crypto-news-integration/domain/entities/matching-config.entity.ts:3,19`; registry `shared/common/persistence/entities.ts:51-91,97`; migraciones espejo `1840000000000-create-crypto-news-publisher-tables.ts`, `1860000000001-DropIngestionOwnedCryptoNewsTables.ts`, `1875000000000-BackfillMatchingConfigFromLlm.ts`; tsconfig `apps/backend/tsconfig.json:14-33` (ojo keys duplicadas settings/* + dead discovery/*: no tocar); jest mapper (backend AGENTS.md TESTS landmine `^telegram/(.*)$`).
  Acceptance criteria (agent-executable): `cd apps/backend && npx tsc --noEmit` exit 0; `cd apps/backend && npm run migration:show` lista `1875000000001` como pendiente (NO `migration:run` aquí — vive en F3 dev DB); `cd apps/backend && grep -c '@Entity' src/threads/*/domain/entities/*.ts` = 8; `cd apps/backend && grep -n 'EXPECTED_ENTITY_COUNT = 47' src/shared/common/persistence/entities.ts` exit 0; `cd apps/backend && grep -c 'IF NOT EXISTS' src/shared/common/persistence/migrations/1875000000001-*.ts` ≥ 8.
  QA scenarios (name the exact tool + invocation): happy: `cd apps/backend && DATABASE_ENABLED=true npx jest src/threads --forceExit` verde (specs mínimos: defaults de las 8 entidades + transiciones 6-estados queue + seed matching 1 row), Evidence `.omo/evidence/task-1-threads-publisher.txt`. failure: `cd apps/backend && grep -rn 'status_code\|targetChannel' src/threads/ || echo CLEAN` → CLEAN; `git diff --stat -- apps/backend/src/telegram apps/backend/src/shared` → vacío salvo `apps/backend/src/shared/common/persistence/entities.ts`; down() verificado por lectura: orden inverso oauth_tokens→matching→throttle→templates→llm→blacklist→keywords→queue (sin tabla media: no existe).
  Commit: Y | feat(threads): scaffold BCs + 8 entidades + migración base
- [x] 2. Publisher application core (enqueue + process-next + schedulers + throttle)
  What to do / Must NOT do: En `src/threads/publisher/application/`: `EnqueueThreadsMessageUseCase` (idempotente + dedup vía `isBlockingFailureReason`, `THREADS_MAX_QUEUE_DEPTH = 100`, PROHIBIDO rechazar por longitud), `ProcessNextThreadsArticleUseCase` (dailyCap default 60, throttle vía SharedThrottleSchedulerService bounds 60s–300s, LLM-or-raw según flags, publish vía ThreadsApiPublisherPort, transiciones 6-estados espejo `VALID_PUBLISH_TRANSITIONS`), `ThreadsPublisherCronScheduler` (`*/10 * * * *`, `THREADS_PUBLISHER_ADVISORY_LOCK_ID=7_421_372`, gate publishingEnabled), `ExpireStaleThreadsScheduler` (`*/30 * * * *`, PENDING >24h→FAILED). Importar ContentFilterService/KeywordRepository-equivalentes threads por puerto; `LlmPort` compartido. MUST NOT: lock distinto de 7_421_372 (assert spec `not.toBeOneOf([7421371,8013203,9421373])`), cap 36, throttle 1msg/min, tocar sender BotApi, enums de 4 estados.
  Parallelization: Wave 2 | Blocked by: 1 | Blocks: 4,5,6 | Can parallelize with: 3
  References: espejos `telegram/crypto-news-publisher/application/handlers/enqueue-matching-message.use-case.ts:46,53`, `process-next-queued-article.use-case.ts:59,82`, `application/scheduling/publisher-cron.scheduler.ts:17,41,69`, `expire-stale-queue-entries.scheduler.ts:25,44`; throttle `telegram/shared/application/services/shared-throttle-scheduler.service.ts:49,30` (+bounds factory `crypto-news-ads.module.ts:127-133`); dedup `shared/deduplication/domain/constants/blocking-failure-reasons.ts:15,52`; `LlmPort` `shared/llm/llm.port.ts:52`; module wiring espejo `crypto-news-publisher.module.ts:90,150-153` (DeduplicationModule forwardRef + LlmPort override con gateway).
  Acceptance criteria: `cd apps/backend && npx tsc --noEmit` exit 0; `cd apps/backend && DATABASE_ENABLED=true npx jest src/threads/publisher/application --forceExit` verde (specs mínimos: enqueue cap/idempotencia, drain happy+BLOCKED+TTL, lock-id assert `not.toBeOneOf([7421371,8013203,9421373])`, 600-char NO-rechazo-en-enqueue); `cd apps/backend && grep -n 'THREADS_MAX_QUEUE_DEPTH = 100' src/threads/publisher/application/handlers/*.ts && grep -n '7_421_372' src/threads/publisher/application/scheduling/*.ts && grep -n '\*/10 ' src/threads/publisher/application/scheduling/*.ts` exit 0.
  QA scenarios (name the exact tool + invocation): happy: `cd apps/backend && DATABASE_ENABLED=true npx jest src/threads/publisher/application --forceExit` verde con adapter mock → PUBLISHED + dailyCap decremente, Evidence `.omo/evidence/task-2-threads-publisher.txt`. failure: lock 7421371 simulado ocupado → skip sin error; texto 600 chars encola igual (rechazo solo pre-publish, todo 3).
  Commit: Y | feat(threads): publisher application core + schedulers
- [x] 3. ThreadsApiPublisherAdapter + token refresher + app.config/validator/templates
  What to do / Must NOT do: `infrastructure/senders/threads-api-publisher.adapter.ts`: `POST /{uid}/threads {media_type=TEXT,text}` → poll `GET /{cid}?fields=status` 3s×10 (`AbortSignal.timeout(10000)`) → `POST /{uid}/threads_publish`; guard pre-publish: `text.length>500` → truncate+`…` (nunca rechazar); strip media (`imagePath`→log `media_skipped`); errores → FAILED con reason (rate-limit/transient = reintentable). `ThreadsTokenRefresher` cron diario (`@Cron('0 0 * * *')`): si `!THREADS_ACCESS_TOKEN` → `logger.warn('THREADS skipped: no token')` + return SIN red; else validar vía `GET .../debug_token` (mask `***` en logs) y luego `GET .../refresh_access_token?grant_type=th_refresh_token` si `expires_in<7d`, upsert `threads_oauth_tokens` id=1. `AppConfig.threads` + `THREADS_ACCESS_TOKEN/USER_ID/POLLING_INTERVAL_MINUTES/DAILY_CAP` placeholders VACÍOS en `.env.staging.template` + `.env.production.template` + entradas `config-validator.ts`. Escribir `docs/threads-token-runbook.md` (setup token 60d paso a paso + refresh + `curl` health + teardown; test -f + `grep -c 'THREADS_'` ≥ 4). MUST NOT: `fields=status_code`, logear tokens (mask `***`), persistir token fuera de `threads_oauth_tokens`, fetch real con FAKE en QA, publicar en prod.
  Parallelization: Wave 2 | Blocked by: 1 | Blocks: 4 (T5 no usa token: enqueue/filter sin red autenticada) | Can parallelize with: 2
  References: spike probado `threads-meta-test/publish.mjs` (2-step, fields=status línea 148, TIMEOUT exit 2) + `exchange.mjs` (form-encoded short POST, long `GET graph.threads.net/access_token?grant_type=th_exchange_token`); env pattern `app.config.ts:166-189,412-413` + `config-validator.ts:110-111` + templates `.env.staging.template:106-107`, `.env.production.template:121-122`; sender espejo `bot-api-crypto-news-publisher.adapter.ts:51` + http-client `:14`.
  Acceptance criteria: `cd apps/backend && npx tsc --noEmit` exit 0; `cd apps/backend && DATABASE_ENABLED=true npx jest src/threads/publisher/infrastructure --forceExit` verde (mock fetch: CREATE→FINISHED→published id; 600 chars→truncado 500; never-finishes→FAILED TIMEOUT; FAKE→REFUSE sin red); `grep -r 'THREADS_' apps/backend/.env* | grep -v template` vacío; `cd apps/backend && grep -n "@Cron('0 0 \* \* \*')" src/threads/publisher/application/scheduling/threads-token-refresher.ts && grep -n 'debug_token' src/threads/publisher/application/scheduling/threads-token-refresher.ts` exit 0; `test -f docs/threads-token-runbook.md` exit 0 (repo root).
  QA scenarios: happy: mock sequence como spike en vivo (container→FINISHED 1er poll), Evidence `.omo/evidence/task-3-threads-publisher.txt`. failure: `cd apps/backend && grep -rn 'status_code\|console.log.*token' src/threads/publisher/infrastructure || echo CLEAN` → CLEAN; `git diff --check` exit 0.
  Commit: Y | feat(threads): Threads API adapter + token refresher + config
- [x] 4. Publisher HTTP (5 controllers + guard prod + route-list test)
  What to do / Must NOT do: `api/http/` espejo exacto con prefijos pineados: `@Controller('threads-publisher/keywords|blacklist|phrases|queue|llm')` (keywords CRUD+batch, blacklist CRUD+batch, phrases+search+conflict-check, queue list/counts/DELETE-cancel, llm config/models/templates CRUD). Guard prod: `PATCH llm/config` con `llmEnabled` → 400 si `NODE_ENV=production` (espejo crypto-news 3-capas: backend guard + seed `llmEnabled=true` + frontend hide en todo 8). Test `app.controller`-style que aserta las 5 rutas registradas. MUST NOT: prefijos `crypto-news-*`, bare `/threads`, exponer token en respuestas (mask), `LlmConfig.matchingEnabled` legacy.
  Parallelization: Wave 3 | Blocked by: 1,2,3 | Blocks: 7,8 | Can parallelize with: 5,6
  References: espejos `api/http/keywords.controller.ts:114,115`, `blacklist.controller.ts:71,72`, `queue.controller.ts:78,79`, `llm-config.controller.ts:71,72`, `phrases.controller.ts:47,48`; guard prod (backend AGENTS.md + `matching-toggle-button.tsx:70`); module wiring `crypto-news-publisher.module.ts:181` (6 entidades + 5 controllers + 2 schedulers).
  Acceptance criteria: `cd apps/backend && npx tsc --noEmit` exit 0; `cd apps/backend && DATABASE_ENABLED=true npx jest src/threads/publisher/api --forceExit` verde; `curl -s -o /dev/null -w '%{http_code}' http://localhost:3030/threads-publisher/queue/counts` → 200 en dev; `curl -s -o /dev/null -w '%{http_code}' -X PATCH http://localhost:3030/threads-publisher/llm/config -H 'Content-Type: application/json' -d '{"llmEnabled":false}'` → 400 con `NODE_ENV=production`, 200 en dev.
  QA scenarios: happy: CRUD keyword roundtrip + queue cancel PENDING→eliminado, Evidence `.omo/evidence/task-4-threads-publisher.txt`. failure: `curl -s -o /dev/null -w '%{http_code}' http://localhost:3030/threads-publisher/keywords` 200 Y `curl -s -o /dev/null -w '%{http_code}' http://localhost:3030/crypto-news-publisher/keywords` 200 (ambos coexisten, ninguno pisa al otro).
  Commit: Y | feat(threads): publisher HTTP + guard prod
- [x] 5. Integration BC (matching + fetch/filter/enqueue + SSE-reuse + polling)
  What to do / Must NOT do: `src/threads/integration/`: `ThreadsMatchingConfig` (repo + seed 1 row matchingEnabled=true) + `@Controller('threads/matching')` config GET|PATCH + `GET threads/matching/health` (contrato 6 campos espejo); `ThreadsIngestionClient` (GET ingestion-service `/api/crypto-news/messages?limit=50`, espejo `CryptoNewsIngestionClient`); `FilteredThreadsService` (`ContentFilterService` + `ChannelFilterRepository` IMPORTADOS tal cual desde `telegram/ingestion/crypto-news` vía su módulo ya público — esos símbolos existen y están exportados (`content-filter.service.ts:30`, `crypto-news-ingestion.module.ts:90`): importar, NO editar; si un método faltara, VENDORIZAR copia en `src/threads` y documentarlo, NUNCA editar `telegram/**` → `ThreadsKeywordRepository` AND-groups → `ThreadsBlacklistPhraseRepository` → matched); `ProcessThreadsMessageHandler` sobre SSE con `messageType='crypto-news'` REUSADO (cero cambios ingestion-service; flag-check → dedup skip PENDING/PUBLISHED/bloqueantes → match → enqueue); `EnqueueThreadsCronScheduler` (5/1 min LITERALES sin leer AppConfig — `AppConfig.threads` (T3) lo espeja para operadores; cero dep en T3). MUST NOT: union `'threads'` nueva, rama coordinator nueva, `EVENT_MAP` nuevo, importar `ChannelContentFilterConfigEntity` directo (servicio/repo por módulo sí, entidad NO), modificar `telegram/ingestion/**`, tablas sources/messages propias.
  Parallelization: Wave 3 | Blocked by: 1,2 (+contrato DAILY_CAP de T2) | Blocks: 7,8 | Can parallelize with: 4,6
  References: espejos `crypto-news-integration.module.ts:91`, `FilteredCryptoNewsService filtered-crypto-news.service.ts:55`, `crypto-news-ingestion-client.service.ts:81`, `process-crypto-news-message.handler.ts:46`, `enqueue-matching-cron.scheduler.ts:43,55,71-80`, `matching-config.entity.ts:19`, `matching-config.controller.ts:50,51`, `matching-health.state.ts:22`, `typeorm-matching-config.repository.ts:9`; puertos `telegram-listener.port.ts:76` + `telegram-sse-listener.adapter.ts:42` (union kol|crypto-news — NO tocar); `ContentFilterService content-filter.service.ts:30,72`; env keys `app.ingestion.useSseCryptoNews` + `app.cryptoNews.pollingIntervalMinutes` (añadir `app.threads.*` espejo).
  Acceptance criteria: `cd apps/backend && npx tsc --noEmit` exit 0; `cd apps/backend && DATABASE_ENABLED=true npx jest src/threads/integration --forceExit` verde (specs mínimos: match AND-groups, blacklist block, dedup skip, flag-off no-enqueue); `git diff --stat -- apps/backend/src/telegram apps/ingestion-service` → vacío.
  QA scenarios: happy: mensaje RAW fixture → matched → queue PENDING, Evidence `.omo/evidence/task-5-threads-publisher.txt`. failure: `matchingEnabled=false` → cero enqueue; evento SSE `messageType='kol'` → ignorado.
  Commit: Y | feat(threads): integration BC + matching + SSE-reuse
- [x] 6. LLM adapter + prompt Threads + templates wiring
  What to do / Must NOT do: `ThreadsLlmAdapter.generateForEntry()` vía `LlmPort` (gateway override espejo `crypto-news-publisher.module.ts:150-153`): prompt Threads que exige salida `<500 chars` texto plano sin formato Telegram (sin HTML/Markdown TG), `templateId|null` fallback, `llmMaxAttempts` de config; seed 1 template `threads-default` con valores literales pineados espejo crypto-news (`model='opencode-zen/deepseek-v4-flash'` de `crypto-news-publisher/infrastructure/config/crypto-news-publisher.config.ts:68`, `maxTokens=2000`, `temperature=0.7` de `llm-config-migration.service.ts:65-66`, `vision=false`; restantes columnas espejo `prompt-template.entity.ts:29`). Reusar `GetLlmModelsUseCase`-equivalente para UI. MUST NOT: formato Bot API (sendPhoto/parse_mode HTML), `targetChannel`, visión obligatoria (TEXT-only MVP: `media_skipped` si hay media).
  Parallelization: Wave 3 | Blocked by: 1,2 | Blocks: — (seed verificado en specs propias; F3-E2E lo consume en wave final) | Can parallelize with: 4,5
  References: espejos `infrastructure/llm/crypto-news-llm.adapter.ts:31,51`, `get-llm-models.use-case.ts:29,36`, `crypto-news-publisher.config.ts:70` (prompt <500), `shared/llm/adapters/llm-gateway.adapter.ts:24`, `LlmPort` `shared/llm/llm.port.ts:52,1`; `LlmConfigMigrationService infrastructure/migration/llm-config-migration.service.ts:6`.
  Acceptance criteria: `cd apps/backend && npx tsc --noEmit` exit 0; `cd apps/backend && DATABASE_ENABLED=true npx jest src/threads/publisher -t 'llm' --forceExit` verde con LLM mock (salida ≤500 chars, template fallback con `templateId=null`, seed threads-default con model/maxTokens/temperature/vision pineados arriba).
  QA scenarios: happy: entry fixture → generatedContent ≤500 sin markup TG, Evidence `.omo/evidence/task-6-threads-publisher.txt`. failure: LLM timeout → FAILED reintentable (no bloqueante, espejo `isBlockingFailureReason`).
  Commit: Y | feat(threads): LLM adapter + prompt + seed template
- [x] 7. Frontend data (entities/threads + features/threads-publisher api/model)
  What to do / Must NOT do: `apps/frontend/src/entities/threads/{api/threads-queries.ts,model/use-threads.ts,index.ts}`: types espejo (`ThreadsQueueEntryView`, filtros), keys factories `threadsPublisherKeys.all=['threads-publisher',...]` + `threadsMatchingKeys.all=['threads','matching',...]` (PROHIBIDO `['threads']` desnudo), endpoints `/threads-publisher/keywords|blacklist|phrases|queue|llm/*` + `/threads/matching/*`, messages/sources/filtros REUSADOS de crypto-news (import, no copia). `features/threads-publisher/{api/{keywords,blacklist,phrases,queue,llm-config}-api.ts,model/use-*.ts}` espejo con polls 10s (matching 5s/15s espejo) + invalidación por factory. Wrappers finos `ThreadsContentFilterManager` etc. con props `{basePath,queryKeys}` (NO as-is). CONTRATO pineado para T8 (firmas literales, return shapes exactos — T8 programa SOLO contra esto):
`useThreadsKeywords(): {data: ThreadsKeywordView[], isLoading: boolean, error: Error|null}` · `useThreadsBlacklist(): {data: ThreadsBlacklistView[], isLoading: boolean, error: Error|null}` · `useThreadsQueue(limit: number, status?: string): {data: ThreadsQueueEntryView[], isLoading: boolean, error: Error|null}` · `useThreadsQueueCounts(): {data: {pending: number, publishedToday: number, remaining: number}, isLoading: boolean}` · `useThreadsMatchingConfig(): {data: {enabled: boolean}, isLoading: boolean}` + `useToggleThreadsMatching(): {toggle: () => void, isPending: boolean}` · `useThreadsMatchingHealth(): {data: ThreadsMatchingHealth | undefined, isLoading: boolean, error: Error|null}` (retry:false; `ThreadsMatchingHealth` = 6 campos espejo `features/crypto-news-publisher/api/llm-config-api.ts:73-80`) · `useThreadsLlmConfig(): {data: ThreadsLlmConfigView, isLoading: boolean}` + `useUpdateThreadsLlmConfig(): {update: (patch: Partial<ThreadsLlmConfigView>) => void, isPending: boolean}` · `useThreadsTemplates(): {data: ThreadsPromptTemplateView[], isLoading: boolean}` + CRUD `useCreateThreadsTemplate/useUpdateThreadsTemplate/useDeleteThreadsTemplate` shape espejo · `useThreadsFilters(channelId: string): {data: ContentFilter[], isLoading: boolean}` (enabled: !!channelId). MUST NOT: copiar queries de messages/sources/filtros, keys desnudas, fetch a `/ingestion-api` nuevo (reusar), `VITE_*` nuevas salvo documentadas.
  Parallelization: Wave 4 | Blocked by: 4,5 (contratos) | Blocks: — (contrato→8) | Can parallelize with: 8
  References: espejos `entities/crypto-news/api/crypto-news-queries.ts:16-169` (keys :76-83, 7 endpoints :85-169), `model/use-crypto-news.ts:18-119` (polls 15s/30s), `features/crypto-news-publisher/api/*` (keywords :52-104, blacklist :52-94, phrases :56-87, queue :45-78, llm-config :30-185), `model/use-{keywords,blacklist,phrases,queue,llm-config}.ts` (polls+invalidación), `shared/api/http-client.ts:14-88`, `shared/api/endpoints.ts:85-97`.
  Acceptance criteria: `cd apps/frontend && npx tsc --noEmit` exit 0; `cd apps/frontend && npx vitest run src/features/threads-publisher` verde (assert URLs empiezan `/threads-publisher/*`, `/threads/matching/*`; cero `/crypto-news*` en api/threads); `cd apps/frontend && grep -rn "\['threads'\]" src/entities/threads src/features/threads-publisher --include='*.ts' | grep -v threads-publisher | grep -v "'threads','matching'" || echo NO-BARE-SINGLE` → NO-BARE-SINGLE; `cd apps/frontend && grep -rn '\["threads"\]' src/entities/threads src/features/threads-publisher --include='*.ts*' || echo NO-BARE-DOUBLE` → NO-BARE-DOUBLE.
  QA scenarios: happy: `cd apps/frontend && npx vitest run src/features/threads-publisher -t 'msw'` hooks contra MSW devuelven listas, Evidence `.omo/evidence/task-7-threads-publisher.txt`. failure: MSW con backend caído (handler `http.get('*', () => HttpResponse.error())`) → hooks retornan `error` visible sin crash; health con `retry:false` como espejo.
  Commit: Y | feat(threads): frontend data + hooks + wrappers
- [x] 8. Frontend página /threads + route/nav + ENDPOINTS + proxy
  What to do / Must NOT do: `pages/threads/index.tsx` espejo del hub (`pages/crypto-news/index.tsx:131-562`): header "Threads" + 6 `<details>` EXACTOS (1 Keywords, 2 Content Filters por canal, 3 Queue+health+toggles, 4 Blocked, 5 LLM config, 6 Prompt Templates — sin Ads); SOLO contra contrato T7-hooks pineado arriba (firmas literales) sin importar implementación T7. Route `{path:'threads'}` en `app/router/routes.tsx:19` + nav `{'/threads','Threads'}` en `root-layout.tsx:7`. `ENDPOINTS.threads` espejo `endpoints.ts:85-97`. Proxy `vite.config.ts`: `/threads-publisher`→:3030 + `/threads/matching`→:3030 (NUNCA `/threads` desnudo, precedente líneas 31-35) + mirror `nginx.conf` locations. Hide toggle LLM si `VITE_APP_ENV==='production'`. `threads-page.test.tsx` (fetch URLs + toggles + 6 sections). Escribir `docs/threads-go-no-go.md` (checklist: post tester id/url, quota, token-solo-.env, tipo cuenta, GO→diseño BC / NO-GO motivo; test -f + `grep -c 'go-no-go'` ≥ 3). MUST NOT: ruta bare `/threads` en proxy, tocar página crypto-news, Ads, copy "crypto-news" en UI, `useSearchPhrases` de crypto-news (usar threads).
  Parallelization: Wave 4 | Blocked by: 4,5 (rutas vivas) + contrato T7 (no código T7) | Blocks: F1–F5 | Can parallelize with: 7
  References: espejos `pages/crypto-news/index.tsx:32-33,46,69-116,131-562`, `routes.tsx:8,19`, `root-layout.tsx:7`, `matching-toggle-button.tsx:10-23,43-138` (3 toggles + hide prod :70), `matching-health-badge.tsx:1,21`, `llm-config.tsx:95-178`, `prompt-templates.tsx:10-18`, `queue-view.tsx:69-454` (DetailsModal compartible por props), `blocked-posts-list.tsx:3-5`, `content-filter-manager.tsx:12-42` (channelId param), `vite.config.ts:17-47`, `nginx.conf:178,214-222`, tests `crypto-news-page.test.tsx:15, queue-view.test.tsx:12, matching-health-badge.test.tsx:14, llm-config.test.tsx:15`.
  Acceptance criteria: `cd apps/frontend && npx tsc --noEmit` exit 0; `cd apps/frontend && npx vitest run src/pages/threads` verde; `curl -s -o /dev/null -w '%{http_code}' http://localhost:5173/threads` → 200 en dev; `curl -s -o /dev/null -w '%{http_code}' http://localhost:5173/threads-publisher/queue/counts` → 200 (llega a :3030, no index.html); `grep -n 'threads-publisher\|threads/matching' apps/frontend/nginx.conf` ≥ 2; `grep -n 'location /threads[^/-]' apps/frontend/nginx.conf apps/frontend/vite.config.ts || echo NO-BARE-PROXY` → NO-BARE-PROXY; `test -f docs/threads-go-no-go.md` exit 0 (repo root).
  QA scenarios: happy: página render 6 secciones + toggles optimistas, Evidence `.omo/evidence/task-8-threads-publisher.txt`. failure: `/threadsXYZ` no interceptado por proxy (route frontend intacta); `VITE_APP_ENV=production` oculta toggle LLM.
  Commit: Y | feat(threads): página /threads + route + proxy

## Final verification wave
> Runs in parallel after ALL todos. ALL must APPROVE. Surface results and wait for the user's explicit okay before declaring complete.
- [x] F1. Plan compliance audit — 8/8 todos `- [x]`; `git status --porcelain | grep -Ev '^(M|A|\?\?) (apps/backend/src/threads/|apps/backend/src/shared/common/(persistence/entities|config/app.config|config/config-validator)\.ts|apps/frontend/src/(entities|features)/threads[^/]*|apps/frontend/src/pages/threads/|docs/threads-|\.env.*template|apps/frontend/(vite.config.ts|nginx.conf)|apps/backend/tsconfig.json)'` → vacío (fuera de esto: solo harness `.omo/`); `git diff --stat -- apps/backend/src/telegram apps/ingestion-service` → vacío; `cd apps/backend && npm run migration:show` sin pendientes threads; `cd apps/backend && grep -n 'EXPECTED_ENTITY_COUNT = 47' src/shared/common/persistence/entities.ts` exit 0.
- [x] F2. Code quality review — `cd apps/backend && npx tsc --noEmit` + `cd apps/frontend && npx tsc --noEmit` exit 0; `grep -rhn 'ADVISORY_LOCK_ID\s*=\s*[0-9_]*' apps/backend/src --include='*.ts' | grep -v spec | sort | uniq -c` → 4 líneas únicas (7421371, 8013203, 9421373, 7421372); `grep -rn 'status_code\|targetChannel' apps/backend/src/threads/ || echo CLEAN1`; `grep -rn "\['threads'\]" apps/frontend/src/entities/threads apps/frontend/src/features/threads-publisher --include='*.ts' | grep -v threads-publisher | grep -v "'threads','matching'" || echo CLEAN2`; `grep -rn 'require(' apps/backend/src/threads/ || echo CLEAN3`; `grep -rn '\["threads"\]' apps/frontend/src/entities/threads apps/frontend/src/features/threads-publisher --include='*.ts*' || echo CLEAN5`; `grep -rn 'THREADS_ACCESS_TOKEN=.\{10,\}' apps/backend/src docs/threads-*.md apps/frontend/src || echo CLEAN4`; `grep -n 'threads-publisher\|threads/matching' apps/frontend/nginx.conf` ≥ 2.
- [x] F3. Agent E2E mock-only (red-ban vigente: cero red Meta) — `cd apps/backend && npm run migration:run` SOLO dev DB + seed; enqueue fixture → drain con adapter mock fetch (CREATE→FINISHED) → PUBLISHED; asserts: dailyCap, 500-truncate, TTL; `DATABASE_ENABLED=true npx jest src/threads --forceExit` verde.
- [x] F4. Humano opt-in manual (EXCEPCIÓN ÚNICA al red-ban; fuera de waves agent) — con token TEST en `apps/backend/.env.test` (jamás commitear): `node threads-meta-test/publish.mjs --text 'spike-verify <ts>'` → post visible en cuenta tester → teardown: borrar post + `rm` test env + `git checkout -- <env si tocado>`; humano confirma + go/no-go (esto BLOQUEA el completo hasta su okay). ⏸️ PAUSADO 2026-09-15: a la espera del humano (sin token TEST disponible para el agente; excepción explícitamente humana). Reanudar con GO/NO-GO del operador.
- [x] F5. Scope fidelity — `test ! -e apps/threads-publisher && echo NO-STANDALONE`; `grep -r 'THREADS_' apps/backend/.env.staging.template apps/backend/.env.production.template | grep -v '=$' || echo TEMPLATES-EMPTY`; `cd apps/backend && DATABASE_ENABLED=true npx jest --forceExit` verde (suite completa, sin conteo hardcodeado) + `cd apps/frontend && npx vitest run` verde; `git diff --check` limpio; `test -f docs/threads-token-runbook.md docs/threads-go-no-go.md` (repo root).

## Commit strategy
- 8 commits atómicos (uno por todo, `feat(threads): …`), rama `feat/threads-publisher` desde `dev`; convención commitlint + hooks vigentes (pre-commit tsc, pre-push tests). Push + PR squash a `dev` solo al final con F1–F5 APPROVE (F4-humano incluido). `threads-meta-test/` queda como evidencia (no se borra hasta go/no-go).

## Success criteria
- Pipeline Threads dev: mensaje ingestion → match threads → queue → LLM → post TEXT real en cuenta tester (id publicado registrado) con guards (500, 250/día implícito dailyCap 60, 10-min drain) en verde.
- `/threads` operativo: keywords/blacklist/queue/toggles/LLM/filtros contra backend, sin tocar `/crypto-news`.
- Cero regresiones crypto-news/ingestion; F1–F5 APPROVE.
