/**
 * @deprecated Hexagonal home is `src/provider/domain/`
 * (Tramo 3, provider-hex). Compat re-export so `provider/*` consumers
 * keep working unchanged. Removed in final review.
 */
export * from './domain/provider-descriptor';
