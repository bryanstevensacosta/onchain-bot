/**
 * @deprecated Hexagonal home is `src/provider/domain/data-provider.port`
 * (Tramo 3, provider-hex). Compat re-export so deep importers keep
 * resolving. Removed in final review — import from the domain barrel.
 */
export * from '../../domain/data-provider.port';
