/**
 * @deprecated Hexagonal home is `src/provider/application/`
 * (Tramo 3, provider-hex). Compat re-export so `provider/*` consumers
 * (address, gateway) keep working unchanged. Removed in final review.
 */
export * from './application/provider-registry.service';
