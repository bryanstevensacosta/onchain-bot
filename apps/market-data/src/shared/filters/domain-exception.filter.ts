/**
 * @deprecated Hexagonal home is
 * `src/shared/infrastructure/filters/domain-exception.filter.ts`
 * (Tramo 3, todo 12, P50). Compat re-export so `shared/*` consumers
 * keep working unchanged. Removed at cutover (todo 8).
 */
export * from '../infrastructure/filters/domain-exception.filter';
