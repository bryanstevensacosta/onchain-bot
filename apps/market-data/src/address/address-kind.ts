/**
 * @deprecated Hexagonal home is `src/address/domain/address-kind.ts`
 * (Tramo 3, todo 12, P50). Compat re-export so `address/*` consumers
 * keep working unchanged. Removed at cutover (todo 8).
 */
export * from './domain/address-kind';
