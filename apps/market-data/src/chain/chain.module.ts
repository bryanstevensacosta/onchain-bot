import { Module } from '@nestjs/common';

/**
 * ChainModule - STUB (Tramo 3, todo 1).
 *
 * Todo 2 lands here: static catalog + EVM/Solana probers + detect-chain
 * use-case (ported reference read-only from backend chain/).
 */
@Module({})
export class ChainModule {}
