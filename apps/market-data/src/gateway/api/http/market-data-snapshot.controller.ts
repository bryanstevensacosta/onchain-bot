/**
 * @deprecated Hexagonal home is
 * `src/gateway/infrastructure/http/market-data-snapshot.controller.ts`
 * (Tramo 3, todo 12, P50). Compat re-export so `gateway/*` consumers
 * keep working unchanged. Removed at cutover (todo 8).
 */
export * from '../../infrastructure/http/market-data-snapshot.controller';
