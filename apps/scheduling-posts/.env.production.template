# =====================================================================
# scheduling-posts PRODUCTION TEMPLATE - placeholders only, NEVER secrets
# =====================================================================
# Copy to apps/scheduling-posts/.env.production on the droplet (gitignored)
# and fill secrets there. DISTINCT secrets per env.
# =====================================================================

NODE_ENV=production

SCHEDULING_POSTS_ENABLED=true
SCHEDULING_POSTS_PORT=4080
SCHEDULING_POSTS_API_KEY=fill-on-droplet

ENCRYPTION_KEY=fill-on-droplet

DATABASE_URL=postgres://onchain_bot:fill-on-droplet@postgres:5432/onchain_bot_scheduling
DATABASE_SYNCHRONIZE=false
DATABASE_LOGGING=false

SCHEDULING_POSTS_UPLOADS_ROOT=/app/apps/scheduling-posts/uploads
SCHEDULING_MEDIA_PUBLIC_BASE_URL=fill-on-droplet

SCHEDULING_TELEGRAM_PUBLISH_DELAY_MS=60000
SCHEDULING_TELEGRAM_DAILY_CAP=20
SCHEDULING_THREADS_PUBLISH_DELAY_MS=60000
SCHEDULING_THREADS_DAILY_CAP=20
SCHEDULING_CRON_ENABLED=true

PUBLISH_RATE_LIMIT_PER_MIN=10

SCHEDULING_SESSION_BINDINGS=fill-on-droplet

SESSION_CALLBACK_URL=fill-on-droplet
SESSION_CALLBACK_API_KEY=fill-on-droplet

BOTS_GATEWAY_URL=http://onchain-bot-telegram-bots-gateway:4070
BOTS_GATEWAY_CLIENT_ID=fill-on-droplet
BOTS_GATEWAY_CLIENT_SECRET=fill-on-droplet

CRYPTO_NEWS_OUTPUT_CHANNEL=fill-on-droplet
THREADS_OUTPUT_CHANNEL=fill-on-droplet
