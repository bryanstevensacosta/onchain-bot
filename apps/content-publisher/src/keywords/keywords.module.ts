import { Module } from '@nestjs/common';

/**
 * KeywordsModule - stub (Tramo 2, todo 1; filled in todo 3).
 *
 * Will own Keyword / BlacklistPhrase entities + OR/AND-group evaluators
 * (moved from backend crypto-news-publisher keywords).
 */
@Module({})
export class KeywordsModule {}
