import { ContractAddress } from 'token/identity/contract-address.vo';

/**
 * @deprecated Use `token/identity/contract-address.vo` instead.
 * This barrel will be removed once all importers are migrated.
 */
export { ContractAddress };
