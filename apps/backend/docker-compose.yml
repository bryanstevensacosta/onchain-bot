services:
  postgres:
    image: postgres:16-alpine
    container_name: onchain-bot-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-onchain}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-onchain}
      POSTGRES_DB: ${POSTGRES_DB:-onchain_bot}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - onchain-bot-pg-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-onchain} -d ${POSTGRES_DB:-onchain_bot}"]
      interval: 5s
      timeout: 3s
      retries: 10

  pgadmin:
    image: dpage/pgadmin4
    container_name: onchain-bot-pgadmin
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL:-admin@admin.com}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:-admin}
      PGADMIN_LISTEN_PORT: 80
      PGADMIN_REPLACE_SERVERS_ON_STARTUP: "True"
      PGADMIN_DISABLE_POSTFIX: "True"
    ports:
      - "${PGADMIN_PORT:-5050}:80"
    volumes:
      - onchain-bot-pgadmin-data:/var/lib/pgadmin
      - ./pgadmin/servers.json:/pgadmin4/servers.json:ro
      - ./pgadmin/preferences.json:/pgadmin4/preferences.json:ro
      - ./pgadmin/queries:/var/lib/pgadmin/storage/admin_admin.com/queries:ro

volumes:
  onchain-bot-pg-data:
    name: onchain-bot-pg-data
  onchain-bot-pgadmin-data:
    name: onchain-bot-pgadmin-data
