import { Module } from '@nestjs/common';

/**
 * ThreadsModule - stub (Tramo 2, todo 1; filled in todo 8).
 *
 * Will own thread-builder/scheduler + 3 use-cases + cron + entities +
 * controller exposing the stub Tramo 1 fixed (same 501s) + the v2
 * un-stubbing CONTRACT (C1). Threads are NEVER implemented here in v1
 * (skeleton + contract only).
 */
@Module({})
export class ThreadsModule {}
