/**
 * Ports (interfaces) for hexagonal architecture
 * 
 * These define contracts that allow transformers to depend on abstractions
 * rather than concrete implementations (dependency inversion principle).
 */

// Ports will be added in subsequent phases (currently empty)
