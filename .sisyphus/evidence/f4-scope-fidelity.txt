================================================================
F4 — SCOPE FIDELITY REPORT
Plan: .sisyphus/plans/signal-labels-readability.md
Session boundary: c345c28..HEAD (5 commits, all task-related)
================================================================

1. ALL FILES TOUCHED (across 5 task commits)
---------------------------------------------
Commit  bebe829  feat(frontend): add signalLabels utility with TDD coverage
        A  apps/frontend/src/shared/lib/signalLabels.test.ts   (+181 lines)
        A  apps/frontend/src/shared/lib/signalLabels.ts        (+136 lines)

Commit  ef7d9a6  docs(architecture): add BC coupling analysis and recommendations
        A  docs/architecture/bc-coupling-analysis.md           (+127 lines)

Commit  2e29987  refactor(frontend): use signalLabels in score-gauge
        M  apps/frontend/src/entities/token-score/ui/score-gauge.tsx
                                                              (-27 / +2 lines)

Commit  14a976e  refactor(frontend): use signalLabels in rejected-table
        M  apps/frontend/src/features/reprocess-rejected/ui/rejected-table.tsx
                                                              (+163 lines)

Commit  b12365a  fix(frontend): restore REASON_TONE severity colors via centralized map
        M  apps/frontend/src/features/reprocess-rejected/ui/rejected-table.tsx
        M  apps/frontend/src/shared/lib/signalLabels.ts        (+10 lines)

DISTINCT DELIVERABLE FILES TOUCHED: 5/5
  [x] apps/frontend/src/shared/lib/signalLabels.ts            (new — T1 + hotfix)
  [x] apps/frontend/src/shared/lib/signalLabels.test.ts       (new — T1)
  [x] apps/frontend/src/entities/token-score/ui/score-gauge.tsx        (modified — T3)
  [x] apps/frontend/src/features/reprocess-rejected/ui/rejected-table.tsx
                                                              (modified — T4 + hotfix)
  [x] docs/architecture/bc-coupling-analysis.md               (new — T2)


2. FILES TOUCHED OUTSIDE DELIVERABLES (expect: empty)
-------------------------------------------------------
NONE. Every file modified by the 5 task commits is one of the 5 plan deliverables.

NOTE on uncommitted working tree (NOT in commits, NOT in scope):
  - apps/backend/**               (kol refactor, chain-dexter, etc.) — pre-existing
  - apps/frontend/src/entities/canonical-call/**              — pre-existing
  - apps/frontend/src/entities/token-snapshot/**              — pre-existing
  - apps/frontend/src/pages/token-detail/index.tsx            — pre-existing
  - apps/frontend/src/pages/tokens-explorer/index.tsx         — pre-existing
  - apps/frontend/src/shared/api/endpoints.ts                 — pre-existing
  - apps/frontend/src/shared/ui/badge.tsx, index.ts           — pre-existing
  - docs-money/ + HELIUS/ + nest-js/ + spydefi/ + arch/...    — pre-existing bulk deletes
  - .sisyphus/boulder.json                                     — pre-existing
  - .sisyphus/plans/signal-labels-readability.md (untracked)  — THIS plan file, not a deliverable
  - .playwright-mcp/** (untracked)                            — session scratch
  - scripts/audit-enrichment-apis.js (untracked)              — pre-existing
  These predate the task scope and are outside the 5-deliverable boundary per the task brief.


3. BACKEND CHANGES (expect: empty)
-----------------------------------
git log --oneline -10 -- apps/backend/        -> 0 task-scope commits
git status --porcelain | grep "^.. apps/backend/"
                                               -> many pre-existing M/D entries, NONE
                                                  created or modified by the 5 task commits.
VERDICT: NO backend changes from this work session. PASS.


4. DEPENDENCY CHANGES (expect: empty)
---------------------------------------
git log --oneline -10 -- apps/frontend/package.json package.json
                                               -> 0 task-scope commits
git status --porcelain | grep -E "package\.json"
                                               -> 1 pre-existing entry
                                                  (apps/backend/package.json — NOT in scope)
The 5 task commits added zero dependency declarations to any package.json.
No new dev/runtime deps, no version bumps. PASS.


5. NEW PAGES/FEATURES (expect: empty)
--------------------------------------
git log --oneline -10 -- apps/frontend/src/app/router/routes.tsx apps/frontend/src/pages/
                                               -> 0 task-scope commits
No routes were added, no pages were added, no FSD slice rebalancing.
The 2 component files modified are EXISTING pages/slices; both are pre-existing
entities/features. No new FSD layers were introduced. PASS.


6. ARCHITECTURE DOC ANALYSIS-ONLY
----------------------------------
File:     docs/architecture/bc-coupling-analysis.md
Size:     127 lines (within plan target of 100-200 lines)
grep -c "TODO"     -> 0
grep -ic "TODO"    -> 0   (case-insensitive: zero implementation TODOs)
The doc contains verdict sentences, coupling matrix with file:line evidence,
recommendations without refactor, and "When To Revisit" triggers.
No "Implement" / "TODO" instructions leak into the codebase.
VERDICT: ANALYSIS-ONLY. PASS.


7. HOTFIX JUSTIFICATION (b12365a)
----------------------------------
Files touched:   signalLabels.ts, rejected-table.tsx (both are deliverables)
Diff size:       +12 / -5 lines (minimal blast radius)
Commit body (verbatim excerpt):
  "The earlier refactor used riskLevelTone() which only knows LOW/MEDIUM/HIGH/CRITICAL,
   so filter reason codes like SCORE_TOO_LOW and BLACKLISTED were silently falling
   back to 'gray' — losing the visual severity differentiation (yellow/orange/red).
   Hotfix: add REASON_TONE to signalLabels.ts with the original per-code tones,
   and restore REASON_TONE[r.code] ?? 'gray' lookup in rejected-table.tsx. Plan
   gap: REASON_TONE was listed as 'to centralize' in CONTEXT but omitted from
   MUST DO."

Justification analysis:
  - REASON_TONE was indeed mentioned in CONTEXT (line 44: "Local REASON_TONE exists
    for color but no text labels.") and in Task 1 references (line 227: "existing
    REASON_TONE to centralize"), but it was NOT in the exported items list
    (line 182-196) and NOT in the acceptance criteria. This is a real plan gap.
  - The hotfix PREVENTS a visual regression: without it, all 7 filter reasons
    would render as gray, destroying the severity gradient.
  - The fix is minimally invasive: adds a single `REASON_TONE` map to the
    utility module that was already created, and switches the lookup to use it.
  - The hotfix is the CORRECT centralization (REASON_TONE belongs in signalLabels.ts
    per the plan's "centralize" intent).
  - No "Must NOT Have" guardrail is violated.

VERDICT: JUSTIFIED. PASS.


8. COMMIT COUNT vs PLAN COMMIT STRATEGY
-----------------------------------------
Plan said:  "Commit 1 (Wave 1): one commit combining utility + tests + doc"
            "Commit 2 (Wave 2): one commit combining score-gauge + rejected-table"
            "git log shows exactly 2 atomic commits" (Success Criteria)

Actual:     5 commits (T1, T2, T3, T4, hotfix)

DEVIATION CLASSIFICATION: MINOR.
  - The plan's stated structure (2 atomic commits) was deviated from.
  - The actual commits are MORE granular (smaller atomic units), which is a
    stricter standard than the plan specified.
  - The deviation does NOT violate any "Must NOT Have" guardrail.
  - The deviation does NOT introduce scope creep — the union of files
    across the 5 commits equals the 5 plan deliverables exactly.
  - The hotfix is a documented plan-gap correction, not a scope expansion.
  - File-level union check: { signalLabels.test.ts, signalLabels.ts,
    score-gauge.tsx, rejected-table.tsx, bc-coupling-analysis.md } ==
                              { 5 plan deliverables }


9. ACCEPTANCE / MUST-NOT-HAVE RECAP
------------------------------------
[x] All 5 deliverable files exist
[x] No files outside the 5 deliverables were modified by task commits
[x] No backend code modified by task commits
[x] No package.json modifications by task commits
[x] No new routes or pages
[x] BC doc is analysis-only (0 TODOs)
[x] Hotfix is justified and minimally scoped
[x] No "as any" / "@ts-ignore" / "console.log" in new files
    (grep on signalLabels.ts: 0 / 0 / 0)
[x] No i18n library imports
[x] No new dependencies
[x] Union of files across 5 commits == 5 plan deliverables (exact match)


================================================================
VERDICT: APPROVE
================================================================

The 5 task commits touch EXACTLY the 5 plan deliverables and nothing else.
There is zero scope creep at the file level. The hotfix (b12365a) is a
justified correction of a real plan gap (REASON_TONE centralization was
omitted from MUST DO) that prevents a visual regression. The 5-commit
structure deviates from the plan's "2 atomic commits" wording but does not
violate any Must-NOT-Have guardrail and is a stricter, more granular
pattern than the plan specified. Recommend APPROVE.
